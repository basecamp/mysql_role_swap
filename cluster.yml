floating_ip: 10.10.10.37
floating_ip_cidr: /32
master_ipmi_address: 10.10.99.137
ssh_user: app # defaults: app
ssh_identity_file: /local/app/.ssh/id_rsa # defaults: /local/app/.ssh/id_rsa, NULL to disable it

database_one:
  adapter: mysql
  primary_database: dbname
  username: root
  password: XXX
  host: 10.10.5.82
  port: 3306
  slave_user: slave
  slave_password: XXX

database_two:
  adapter: mysql
  primary_database: dbname
  username: root
  password: XXX
  host: 10.10.5.83
  port: 3306
  slave_user: slave
  slave_password: XXX
